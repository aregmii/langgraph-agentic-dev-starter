server:
  port: 8080

agent-service:
  url: http://localhost:8000

spring:
  application:
    name: gateway-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.codeagent: DEBUG
